<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
   android:id="@+id/root_view"
   android:layout_width="match_parent"
   android:layout_height="wrap_content"
   android:paddingTop="4dp"
   android:paddingBottom="4dp"
   android:background="@color/primary_light"
   android:clickable="false"
   android:focusable="false"
   android:focusableInTouchMode="false"
   android:descendantFocusability="blocksDescendants"
   android:paddingStart="@dimen/list_item_margin_sides"
   android:paddingEnd="@dimen/list_item_margin_sides"
   android:gravity="center_vertical">

   <TextView
      android:id="@+id/tv_value"
      style="@style/Text_GraySmall"
      android:textColor="#ffffff"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_centerVertical="true"
      android:layout_alignParentEnd="true"
      android:layout_alignParentRight="true"
      android:text="40" />


   <TextView
      android:id="@+id/tv_key"
      style="@style/Text_GraySmall"
      android:textColor="#ffffff"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_centerVertical="true"
      android:singleLine="true"
      android:ellipsize="end"
      android:paddingRight="8dp"
      android:text="Velerian Antovachoski von Vanona und rider"
      android:layout_toStartOf="@id/tv_value"
      android:layout_toLeftOf="@id/tv_value"
      />

</RelativeLayout>